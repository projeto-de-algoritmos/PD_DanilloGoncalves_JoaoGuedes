class Items{
    constructor(){
        this.values = [];
        this.weights = []; 
    }
    setValues(values){
        this.values = [...values];
    }
    setWeights(weights){
        this.weights = [...weights];
    }
}